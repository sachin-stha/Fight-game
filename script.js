const canvas = document.querySelector("#myCanvas");
const ctx = canvas.getContext("2d");

let canvasSize = { width: 1000, height: 600 };

canvas.width = canvasSize.width;
canvas.height = canvasSize.height;
